server:
  port: 8080
  servlet:
    context-path: /social

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clipped
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
